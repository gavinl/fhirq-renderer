name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

        run: |
          npm install
          npx eslint .
          npm test -- --watchAll=false
          npm build
