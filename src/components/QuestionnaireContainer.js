import React, { useEffect } from "react";
import Questionnaire from "./Questionnaire";

const QuestionnaireContainer = ({ questionnaire }) => {
  <Questionnaire questionnaire={questionnaire} />;
};

export default QuestionnaireContainer;
